# DocTrack Frontend Style Guide
1. Strictly use Tailwind CSS utility classes. Avoid inline styles (`style={{...}}`).
2. All components must be Functional Components using hooks.
3. Variable names must be highly descriptive (e.g., use `trackingNumberLabel` not `lbl`).